{
  "name": "student-manager",
  "version": "1.0.0",
  "private": true,
  "description": "极简学员信息管理网页，支持本地 JSON 文件存储和 Cloudflare KV 存储。",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js && node --check worker.mjs && node --check functions/_shared/storage.js && node --check functions/api/students.js && node --check functions/api/students/[id].js && node --check functions/api/statuses.js"
  },
  "engines": {
    "node": ">=18"
  }
}